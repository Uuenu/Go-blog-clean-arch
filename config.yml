---

is_degub: true
listen:
  type: port
  bind_ip: 0.0.0.0
  port: 10000
mongodb:
  host: localhost
  port: 27017
  database: user-service
  authDb: 
  username:
  password:
  collection: users
session:
  ttl: 60m
  cookie_key: "id"
  cookie_domain: ""
  cookie_secure: false
  cookie_httponly: true

